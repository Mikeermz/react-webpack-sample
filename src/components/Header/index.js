import React, { Component } from 'react';
import Footer from '../Footer';
const  Header =({message})=> (
<div>    
    <div className="titulo"><h1>{message}</h1></div>
    
</div>)
export default Header;